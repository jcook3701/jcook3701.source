# meson-install

[messon-install](https://github.com/keithbowes/meson-install) is designed to allow for the easy creation of packages from Meson-based projects using checkinstall. It uses regular expressions to convert meson install or muon install output into a format compatible with checkinstall, acting as a substitute for make install in traditional autotools workflows.
